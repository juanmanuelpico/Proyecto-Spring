spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sistema_iot
    username: root 
    password: root
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create
      naming:
        strategy: org.hibernate.cfg.ImprovedNaming.Strategy
    properties:
      dialect: org.hibernate.dialect.MySQL8Dialect
  thymeleaf:
    cache: false      